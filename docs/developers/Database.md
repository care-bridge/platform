# Database

## Tables

1. users (donors + admins)
2. hospitals (note: it has 'status' column that get changed ONCE when admin approves it's signup request) 
3. requests
4. donations
5. follows (followings only* donor can follow hospitals)
6. notifications (donors + admins + hospitals)
